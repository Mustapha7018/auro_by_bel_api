fastapi
uvicorn[standard]
sqlmodel
pydantic[email]
pydantic-settings
PyJWT
bcrypt
google-auth
requests
boto3
python-multipart
# Postgres driver for production (Render). The marker skips it on 3.14 where
# there's no wheel yet — local dev uses SQLite and doesn't need it.
psycopg[binary]; python_version < "3.14"
